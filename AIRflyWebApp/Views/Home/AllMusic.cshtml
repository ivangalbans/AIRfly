


    @{

        foreach (var v in ViewBag.songs)
        {
            @*var dict =
                new Dictionary<string, string>
                {
                {"song", v},
                };
            <li><a asp-area="" asp-all-route-data="dict" asp-controller="DataTransfer" asd=@v asp-action="GetFile">@v</a></li>*@
            <h5>@v</h5>
            <audio controls=""   name="media"><source src="http://localhost:12465/getfile/@v"></audio>
            <br/>
        }
    }

<form method="post" enctype="multipart/form-data" asp-controller="DataTransfer" asp-action="Post">
    <div class="form-group">
        <div class="col-md-10">
            <p>Upload one or more files using this form:</p>
            <input type="file" name="files" multiple />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Upload" />
        </div>
    </div>
</form>